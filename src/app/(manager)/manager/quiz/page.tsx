import React from "react";
import UnderDev from "@/components/under-dev";

const page = () => {
  return (
    <div>
      <UnderDev featureName="Quiz" message="Can be used to Manage Quizzes" />
    </div>
  );
};

export default page;
